{
  "name": "storagedaddy-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "node --test worker.test.mjs analytics.test.mjs clarity.test.mjs && tsc --noEmit && wrangler deploy --dry-run",
    "deploy": "node check-release.mjs && npm run check && wrangler deploy --tag \"$(git rev-parse HEAD)\""
  },
  "devDependencies": {
    "wrangler": "4.125.0",
    "typescript": "6.0.3"
  }
}
